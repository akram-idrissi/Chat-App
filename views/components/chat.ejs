<%- include('../header') %>
    <p x-data="{show:true}" x-init="setTimeout(() => show=false, 60000)" x-show="show" x-transition
        class="fixed bg-blue-500 text-white py-2 px-4 rounded-xl z-20 bottom-20 right-6 font-bold">Welcome Back, Akram :)
    </p>

    <div class="h-screen w-full flex antialiased text-gray-200 bg-second overflow-hidden">
        <div class="flex-1 flex flex-col">
            <main class="flex-grow flex flex-row min-h-0">
                <!-- left -->
                <%- include('left-part') %>
                    <!-- middle -->
                    <%- include('middle-part') %>
            </main>
        </div>
    </div>
    <%- include('../footer') %>